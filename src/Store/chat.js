import { createSlice } from "@reduxjs/toolkit";


const initialState = {

}

const chatSlice = createSlice({
    initialState,
    name:'chatSlice',
    reducers:{
      setUpdate:()=>{
        
      }
    }
})


export default chatSlice.reducer;

